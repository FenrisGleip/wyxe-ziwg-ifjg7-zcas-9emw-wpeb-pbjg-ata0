# Auto-generated by CIPHER v5.0
# Source: 2af63d5411bb
attack_technique: T1021.006
display_name: "T1021.006 - Remote Services: SQL Server (Use Extracted Credentials)"
atomic_tests:
  - name: "T1021.006 - Remote Services: SQL Server (Use Extracted Credentials)"
    description: |
      Use the credentials harvested from the local cache to connect to the backend SQL Server and run a harmless query.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $srv = "localhost";
        $uid = "iconics_user";   # replace with extracted UID
        $pwd = "P@ssw0rd!";      # replace with extracted PWD
        Invoke-Sqlcmd -ServerInstance $srv -Username $uid -Password $pwd -Query "SELECT name FROM sys.databases;"
      cleanup_command: |
        # No cleanup needed for read‑only query
