# Auto-generated by CIPHER v5.0
# Source: 70f63cfc6a82
attack_technique: T1129
display_name: "T1129 - Solana C2 Address Extraction"
atomic_tests:
  - name: "T1129 - Solana C2 Address Extraction"
    description: |
      Extract C2 server URL from a Solana transaction log.
    supported_platforms:
      - linux
    executor:
      name: python
      command: |
        import json, sys
        from solana.rpc.api import Client
        client = Client("https://api.mainnet-beta.solana.com")
        addr = "Ft5fbkqNa76vnsjYNwjDZUXoTWpP7VYm3mtsaQckQADN"
        sigs = client.get_signatures_for_address(addr, limit=5)['result']
        c2 = None
        for s in sigs:
            tx = client.get_transaction(s['signature'], encoding='json')
            logs = tx['result']['meta'].get('logMessages', [])
            for line in logs:
                if 'C2://' in line:
                    c2 = line.split('C2://')[1].strip()
                    break
            if c2:
                break
        print(c2 or 'Not found')
      cleanup_command: |
        echo "No cleanup needed"
