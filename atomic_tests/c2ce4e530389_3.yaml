# Auto-generated by CIPHER v5.0
# Source: c2ce4e530389
attack_technique: T1068
display_name: "T1068 - Bypass DEP via ROP in Proteus"
atomic_tests:
  - name: "T1068 - Bypass DEP via ROP in Proteus"
    description: |
      After the OOB write, the embedded ROP chain calls VirtualProtect to make the shellcode executable.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        # No explicit command – verification step
        Get-Process -Name proteus | Where-Object {$_.Modules -match 'calc.exe'}
      cleanup_command: |
        Stop-Process -Name calc -Force
